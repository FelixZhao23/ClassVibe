//
//  Models.swift
//  ClassVibe
//
//  Created by cmStudent on 2026/01/20.
//



import Foundation

// ==========================================
// 1. 课程模型
// ==========================================
struct Course: Identifiable {
    let id: String          // 课程唯一ID (Firebase Key)
    let title: String       // 课程标题 (例如: "Java基础")
    let teacherName: String // 老师名字或ID
    let isActive: Bool      // 是否正在上课
}

// ==========================================
// 2. 奖励物品模型 (用于扭蛋)
// ==========================================
struct RewardItem: Identifiable {
    let id = UUID()         // 物品唯一ID (本地生成)
    let name: String        // 物品名称 (例如: "免作业券")
    let rarity: String      // 稀有度 (SSR, SR, R, N)
    let icon: String        // 图标 emoji (例如: "👑")
}

// ==========================================
// 3. 游戏模式枚举
// ==========================================
enum GameMode {
    case normal     // 正常模式
    case fever      // 🔥 全员狂热 (积分翻倍+特效)
    case battle     // ⚔️ 红蓝对决 (分组对抗)
}

// ==========================================
// 4. 对战队伍枚举
// ==========================================
enum Team {
    case red        // 红队
    case blue       // 蓝队
    case none       // 未分组
}

// ==========================================
// 5. 馒头精心情枚举
// ==========================================
enum PetMood {
    case sleepy     // 没人按 (睡觉)
    case happy      // 正常 (开心)
    case superHappy // 气氛热烈 (星星眼)
    case confused   // 困惑 (晕)
    case panic      // 恐慌 (哭)
}
